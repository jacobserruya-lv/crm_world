/**
 * This dumb class is intented to overwrite a class that we can't delete on prod
 */

 @istest
public with sharing class TWIST_UtilsServices_TEST {
    @isTest
    public static void test() {
        System.assert(true);
    }
}