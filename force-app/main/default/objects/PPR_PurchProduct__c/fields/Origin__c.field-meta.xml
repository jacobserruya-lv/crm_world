<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Origin__c</fullName>
    <description>Order created by which system</description>
    <externalId>false</externalId>
    <formula>IF (
  /* Transaction id starting by 4 numbers (store code) =&gt; X-Store */
  AND (
    NOT(ISBLANK(TransactionRMSId__c)),
    ISNUMBER(LEFT(TransactionRMSId__c, 4))
  ),
  &apos;X-STORE&apos;,
  IF (
    /* Transaction id starting by 1 letter (store code) =&gt; RMS */
    AND (
      NOT(ISBLANK(TransactionRMSId__c)),
      NOT(ISNUMBER(LEFT(TransactionRMSId__c, 1)))
    ),
    &apos;RMS&apos;,
    /* Transaction id empty */
    &apos;&apos;
  )
)</formula>
    <label>Origin</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
