<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Exclude_SFMC__c</fullName>
    <description>If one of these conditions is verified, the Account will be flagged (Excluded for SFMC)</description>
    <formula>OR(ISPICKVAL(ParallelMarket_Status__pc , &apos;Blue&apos;), ISPICKVAL(ParallelMarket_Status__pc , &apos;Red&apos;),
!ISBLANK(Anonymization_key__c))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Exclude SFMC</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
