<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Days_to_event__c</fullName>
    <externalId>false</externalId>
    <formula>MIN(
IF(MONTH(PersonBirthdate)=2 &amp;&amp; DAY(PersonBirthdate)=29,
IF(ISBLANK(
DATE(YEAR(TODAY()), MONTH(PersonBirthdate), DAY(PersonBirthdate)-1) - TODAY()
), 400, 
DATE(YEAR(TODAY()), MONTH(PersonBirthdate), DAY(PersonBirthdate)-1) - TODAY()
),
IF(ISBLANK(
DATE(YEAR(TODAY()), MONTH(PersonBirthdate), DAY(PersonBirthdate)) - TODAY()
), 400, 
DATE(YEAR(TODAY()), MONTH(PersonBirthdate), DAY(PersonBirthdate)) - TODAY()
))
,
IF(MONTH(Anniversary__pc)=2 &amp;&amp; DAY(Anniversary__pc)=29,
IF(ISBLANK(
DATE(YEAR(TODAY()), MONTH(Anniversary__pc), DAY(Anniversary__pc)-1) - TODAY()
), 400, 
DATE(YEAR(TODAY()), MONTH(Anniversary__pc), DAY(Anniversary__pc)-1) - TODAY()
),
IF(ISBLANK(
DATE(YEAR(TODAY()), MONTH(Anniversary__pc), DAY(Anniversary__pc)) - TODAY()
), 400, 
DATE(YEAR(TODAY()), MONTH(Anniversary__pc), DAY(Anniversary__pc)) - TODAY()
))
)</formula>
    <label>Days to event</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
