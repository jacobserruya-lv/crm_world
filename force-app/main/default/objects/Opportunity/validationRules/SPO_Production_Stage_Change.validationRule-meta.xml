<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>SPO_Production_Stage_Change</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    ISCHANGED(StageName),
    OR(
        ISPICKVAL(StageName, &apos;In progress&apos;),
        ISPICKVAL(StageName, &apos;Distribution in Progress&apos;)
    ),
    ISPICKVAL(ApplicationSource__c, &apos;SPO&apos;),
    NOT($Profile.Name = &apos;SPO_Production&apos;),
    NOT($Profile.Name = &apos;LV_Interface_Datastage&apos;), 
    NOT($Profile.Name = &apos;System Administrator&apos;), 
    NOT($User.BypassVR__c)
)</errorConditionFormula>
    <errorMessage>Only the Production Team can advance the stage</errorMessage>
</ValidationRule>
