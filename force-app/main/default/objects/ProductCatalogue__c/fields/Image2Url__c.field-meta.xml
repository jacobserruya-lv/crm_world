<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Image2Url__c</fullName>
    <externalId>false</externalId>
    <formula>IF (NOT(ISBLANK(FluidImageRecipeId__c)),
  $Setup.ProductSettings__c.FluidPictureUrl__c + FluidImageRecipeId__c + &apos;/image/Back.jpg&apos;,
  IF (NOT(ISBLANK(Image2Name__c)),
    $Setup.ProductSettings__c.BccLargePictureUrl__c + Image2Name__c, 
    IF (NOT(ISBLANK(FluidRecipeId__c)),
      $Setup.ProductSettings__c.FluidPictureUrl__c + FluidRecipeId__c + &apos;/image/Back.jpg&apos;,
      &apos;&apos;
    )
  )
)

/*IF (
  NOT(ISBLANK(FluidRecipeId__c)),
  $Setup.ProductSettings__c.FluidPictureUrl__c + FluidRecipeId__c + &apos;/image/Back.jpg&apos;,
  IF (
    NOT(ISBLANK(Image2Name__c)),
    $Setup.ProductSettings__c.BccLargePictureUrl__c + Image2Name__c, 
    &apos;&apos;
  )
)*/</formula>
    <label>Image 2 Url</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
