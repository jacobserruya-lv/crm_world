({
/*	fireColorSelected : function(component) {
        if (!component.get("v.readOnly")) {
            this.toggleColorSelected(component);
           //var divId = component.find("tileDiv");
          // $A.util.addClass(divId, 'select');//.siblings().removeClass("select");

            //var myEvent = $A.get("e.c:SO_ColorPickerTileEvent");
            //var myEvent = component.getEvent("colorTileChange");
            var myEvent = $A.get("e.c:SO_ColorPickerTileEvent");
            myEvent.setParams({
                "color": component.get("v.color"),
                "colorUrl": component.get("v.colorUrl"),
                "colorBackground": component.get("v.colorBackground"),
                "type": component.get("v.type"),
                "material": component.get("v.material")
                //channel: "ColorPicker"
            });
            myEvent.fire();
        }
    },
    
	toggleColorSelected : function(component) {
        var divId = component.find("tileDiv");
   //     console.log('divId', divId);
        $A.util.addClass(divId, 'select');//.siblings().removeClass("select");
           // source.addClass("selected").siblings().removeClass("selected");
    }*/
})